# Settings for Debugging / Testing
weatherapi.local=true
weatherapi.fake-weather=true
runs.fake-generation.enabled=true

# JWT - both read from the environment, no committed values. Set JWT_SECRET
# before running any profile (see README for local dev setup).
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=${JWT_EXPIRATION_MS:3600000}
